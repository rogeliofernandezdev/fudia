"use client";
import "./styles/confirm-dialog.css";
import {Icon} from "@/design-system/icons";
export function ConfirmDialog({open,title,description,tone="danger",confirmLabel,pending=false,onCancel,onConfirm}:{open:boolean;title:string;description:string;tone?:"danger"|"success";confirmLabel:string;pending?:boolean;onCancel:()=>void;onConfirm:()=>void}){if(!open)return null;return <div className="confirm-overlay"><section className="confirm-card" role="alertdialog" aria-modal="true" aria-labelledby="confirm-title"><span className={`confirm-icon ${tone}`}><Icon name={tone==="danger"?"alert":"check"}/></span><h2 id="confirm-title">{title}</h2><p>{description}</p><footer><button className="button ghost" disabled={pending} onClick={onCancel}>Cancelar</button><button className={`button ${tone}`} disabled={pending} onClick={onConfirm}><Icon name={tone==="danger"?"alert":"check"} size={16}/>{pending?"Procesando...":confirmLabel}</button></footer></section></div>}
