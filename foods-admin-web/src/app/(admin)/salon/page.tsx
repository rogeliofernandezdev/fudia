import type {Metadata} from "next";
import {SalonManager} from "@/components/salon-manager";
export const metadata:Metadata={title:"Salón — fudIA Admin"};
export default function Page(){return <SalonManager/>}
