# PWA y operación offline

Manifest, iconos y service worker versionado. El shell puede almacenarse. Offline
no autoriza pagos, comprobantes ni cierres. Toda cola local usa idempotencia,
estado visible y reconciliación explícita.
